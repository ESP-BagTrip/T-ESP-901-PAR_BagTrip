---
# Ansible Galaxy collections required by the playbooks.
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: ansible.posix
    version: ">=1.6.0"
  - name: community.general
    version: ">=9.0.0"
  - name: community.docker
    version: ">=3.10.0"
